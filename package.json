{
  "name": "ado-boards-insights",
  "version": "0.2.0",
  "scripts": {
    "build": "webpack",
    "package": "npm run build && tfx extension create --manifest-globs vss-extension.json",
    "package:dev": "npm run build && tfx extension create --manifest-globs vss-extension.json vss-extension.dev.json"
  },
  "engines": {
    "node": ">=24",
    "npm": ">=10"
  },
  "devDependencies": {
    "tfx-cli": "^0.17.0",
    "ts-loader": "^9.0.0",
    "typescript": "^4.0.0",
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0"
  },
  "dependencies": {
    "azure-devops-extension-api": "^4.270.0",
    "azure-devops-extension-sdk": "^4.2.0"
  }
}
